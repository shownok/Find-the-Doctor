@model IEnumerable<appweb.Models.Specialist>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SpecialistType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SpecialistType)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SpecialistId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SpecialistId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SpecialistId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
